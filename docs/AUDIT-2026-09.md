# Stabilization audit — September 2026

A repository-wide review of Meno at `main` @ `25a4b7a`, carried out before the
second stabilization PR. Each finding is rated on two axes:

- **Severity** — user or developer impact if left alone (High / Medium / Low).
- **Fix risk** — how likely a fix is to regress behaviour or GUI feel
  (Low / Medium / High).

Status: **PR** = addressed in the stabilization PR that adds this document;
**#2** = covered by the open PR #2; **Open** = backlog / issue candidate.

Baseline at the time of the audit: `npm run build` passed, `npm run lint`
passed with 13 warnings, there were no frontend tests, and no CI. Rust could not
be compiled on the audit machine (no MSVC build tools), which is why Rust-side
findings are left open and are verified through CI instead.

## Tooling and build

| ID | Finding | Sev | Risk | Status |
| --- | --- | --- | --- | --- |
| T1 | `zustand`, `three-stdlib` and `framer-motion` are imported directly but only present as transitive dependencies (of `@react-three/fiber`, `@react-three/drei`, `motion`). A dependency bump upstream can break the build. | High | Low | PR |
| T2 | No automated frontend tests and no `typecheck` / `test` npm scripts; parsers and the tab reducer are pure functions that are easy to unit-test. | Medium | Low | PR |
| T3 | No CI: nothing checks typecheck, lint, tests, build or `cargo clippy` on pull requests. | Medium | Low | PR |
| T4 | Rust: the `which` crate is declared but unused; the template `greet` command is registered but never called. | Low | Low | Open |
| T5 | `npm audit` reports transitive advisories (`nanoid` via PostCSS, `postcss`, `fflate` via `three-stdlib`); all are fixable with `npm audit fix` but change the lockfile, so they belong in a separate dependency PR. | Medium | Low | Open |
| T6 | ESLint reports 13 warnings (0 errors): 9 `react-hooks/exhaustive-deps` in StructureEditor components and 4 `react-refresh/only-export-components` in the editor store. Each dependency list needs individual review — adding deps blindly can cause render loops in the R3F layers. | Low | Medium | Open |
| T7 | The production bundle is a single ~1.7 MB chunk (Vite warns > 500 kB). Views could be code-split per tab kind. | Low | Low | Open |
| T8 | `npm run tauri build` failed outright: the Rust crates in `Cargo.lock` (tauri 2.8.2, tauri-plugin-fs 2.4.2) were older than the installed npm packages (2.11.1 / 2.5.1) and the Tauri CLI refuses to bundle on a major/minor mismatch. `tauri dev` only warns, and CI never ran a release build, so no installer could be produced. | High | Low | Fixed (tauri build PR): crates updated and a `check:tauri` guard added to CI. |

## Correctness bugs

| ID | Finding | Sev | Risk | Status |
| --- | --- | --- | --- | --- |
| B1 | Format detection treats any file whose first line is an integer as XYZ, before looking for MOL markers. MOL/SDF files whose title line is a number — e.g. PubChem SDFs, which use the CID as the title — are routed to the 3D viewer and parsed as XYZ, producing garbage. Affects both `detectFormat` and OmniHub's own `isXyz`. | High | Low | PR |
| B2 | RXN reactant/product counts are read from the first header line containing two integers. A reaction name such as `step 1 of 2` is mistaken for the counts line, so molecules are assigned to the wrong side of the arrow. The CTfile spec fixes the counts line at line 5. | Medium | Low | PR |
| B3 | The 2D editor keeps a module-level store registry keyed by `tabId`. The workflow editor passes its React Flow node id (`"mol2d"`) as `tabId`, so **every Workflow Builder tab shares one sketch store**, and closing one tab deletes the registry entry the others still use. | High | Low | PR |
| B4 | Python console: event listeners are registered asynchronously inside an effect keyed on `pid`. Output emitted between spawn and re-subscription (e.g. the auto-run `AUTO OK`) is dropped, a fast cleanup can leak listeners (duplicated lines under React StrictMode), and a failed `Start` (missing `uv`, network error) gives no feedback. | Medium | Low | PR |
| B5 | `StructureCanvas` hardcodes `active={true}`, so every 2D tab keeps its render loop running at DPR ≥ 2 while hidden. The `active` flag already exists and the 3D viewer uses the same pattern. | Medium | Low | PR |
| B6 | `Bonds2D` sizes its instanced mesh for 3 segments per bond; hashed wedges and wavy bonds need more, overflowing the buffer and blanking the canvas. | High | Low | #2 |
| B7 | Rust: when a sidecar exits by itself its `Child` stays in `ProcState` and is never waited on (handle leak); running sidecars are not killed when the app exits. | Medium | Low | Fixed (sidecar hardening PR) |
| B10 | Rust: log/stdout readers did blocking reads inside async tasks, occupying async worker threads for the child's lifetime (on a low-core machine the uv log pipes could starve and stall `uv`). `py_env_setup_uv` still blocks one async worker while waiting for `uv`. | Medium | Low | Readers fixed (sidecar hardening PR); the wait is open |
| B8 | MDL bond stereo code 3 ("cis/trans either" on double bonds) is mapped to a wavy single-bond depiction. Needs a chemistry decision (crossed double bond?). | Low | Medium | Open |
| B9 | Import failures only reach `console.warn`; the user sees nothing. `.pdb` and `.ket` are offered in the file picker but no parser exists. | Medium | Medium | Fixed in the 2D editor (import feedback PR): errors are shown on the canvas, PDB/KET say "not supported yet". |

## Security (Tauri commands and process execution)

| ID | Finding | Sev | Risk | Status |
| --- | --- | --- | --- | --- |
| S1 | `ext_spawn_sidecar` spawns **any absolute path with any arguments** passed from the webview. `py_env_setup_uv` executes a `uv` path supplied by the frontend and joins `venvHome` (which may contain `..`) onto the app data dir. Any script injection in the webview therefore becomes arbitrary command execution. (The console runs arbitrary Python by design, so this is defense-in-depth, but it removes the easiest path.) | High | Medium | Fixed (sidecar hardening PR): paths are validated in Rust without changing the command contract. Replacing paths with enums (A5) is still worth doing. |
| S2 | `app.security.csp` is `null`. A CSP would limit the impact of any HTML/script injection. Needs GUI testing: motion/Tailwind use inline styles, textures may use `blob:`/`data:`. | Medium | Medium | Open |
| S3 | `Mutex::lock().unwrap()` in the sidecar commands turns a poisoned lock into a panic on every later call. | Low | Low | Fixed (sidecar hardening PR) |
| S4 | File-system capabilities are scoped to `$RESOURCE` / `$APPDATA` / `$APPLOCALDATA`; no shell plugin is enabled. Reasonable as is. | — | — | OK |

## GUI and interaction

| ID | Finding | Sev | Risk | Status |
| --- | --- | --- | --- | --- |
| G1 | The 2D editor contains a hidden "Open (replace)" file input that nothing triggers; replacing a structure is unreachable (only drag-and-drop append works). | Medium | Low | Fixed (import feedback PR): an Open button next to Fit. |
| G2 | Tabs close on `pointerdown` of the × button rather than on click, making accidental closes easy; dirty tabs close without confirmation (there is no save yet). | Medium | Low | Open |
| G3 | "New…" menu badges (`new`, `accel`, `test`) are internal jargon; the logo button does nothing; tabs are not keyboard-focusable. | Low | Low | Open |
| G4 | The workflow editor is a static demo: parameter changes are not stored, nothing runs, and the 3D node shows a bundled sample XYZ. It is not labelled as a preview in the UI. | Medium | Low | Open |
| G5 | All tabs stay mounted; each 2D/3D tab (and each workflow tab, which embeds two canvases) holds a WebGL context. Browsers cap contexts at roughly 16, after which the oldest canvas is lost. `ViewEntry.keepAlive` exists but is unused. | Medium | Medium | Capped (render load PR): opening past 16 canvases is refused with a notice instead of silently losing a view. Unmounting hidden views is still open and needs per-tab state to move out of the view. |
| G6 | `useCanvasSetup` forces `setPixelRatio(max(dpr, 1.5))`, disagreeing with the canvas's `dpr` prop (≥ 2); its `getContext("2d")` on a WebGL canvas always returns `null`. | Low | Medium | Fixed (on-demand rendering PR): the maintainer compared 1.5x and 2.0x and chose 2.0x, now a single `CANVAS_DPR` constant; the override and the dead 2D-context code are gone. |
| G7 | 3D viewer measurements and selections are atom indices that persist when switching frames/files. | Low | Low | Open |
| G8 | Window has no minimum size; the custom title bar collapses when shrunk. | Medium | Low | #2 |
| G9 | Fit-to-content has no zoom ceiling: a small molecule (e.g. a single C–O bond) is zoomed until the bond and label fill the whole canvas. | Low | Low | Open |
| G10 | The canvases embedded in workflow nodes (2D sketch, 3D viewer) are not told when their tab is hidden and keep rendering; the B5 fix covers standalone 2D tabs only. | Low | Low | Fixed (render load PR) |
| G11 | The 2D editor redrew continuously (60 fps) whenever it was visible, even with nothing moving, and `AromaticCircles2D` re-rendered React on every frame. | High | Medium | Fixed (on-demand rendering PR): the canvas renders on demand; interactions and animations request frames. |
| G12 | The 3D viewer still redraws continuously while its tab is active, even when the molecule is not moving. The same on-demand treatment needs the trackball damping and the atom/bond animations to request frames. | Medium | Medium | Open |

## Architecture and coupling

| ID | Finding | Sev | Risk | Status |
| --- | --- | --- | --- | --- |
| A1 | Four parallel molecule type families: `utils/structureParsers` (`Molecule`), `utils/importers` (`EditorModel`), the editor store's `Atom`/`Bond`, and `lib/chem/layout2d`'s `Atom`/`Bond`. Conversions are ad hoc and use `as any`. Suggest one core chemistry model in `src/lib/chem`. | Medium | High | Open |
| A2 | Tab `content.data` is `unknown` and every view casts it with `as any`. A discriminated union per `TabKind` would make the registry type-safe. | Medium | Medium | Open |
| A3 | Duplicated code: `replayReplace`/`replayAppend` in `useStructureEvents`; `moleculesToEditorModel` re-implements `computeScaleForMols`; OmniHub re-implements format sniffing (removed by the B1 fix). | Low | Low | Partly PR |
| A4 | Frontend/backend contract is stringly typed: command names and payload shapes are repeated by hand, and events carry JSON encoded as a string. Suggest a single typed wrapper module (`src/lib/tauri/`) and typed event payloads. | Medium | Medium | Open |
| A5 | Python env logic is split: TypeScript computes lock hashes and stamp files, Rust runs `uv`. Moving it all to Rust would also fix S1. | Medium | Medium | Open |
| A6 | Very large components: `Atoms2D.tsx` (~1k lines), `GraphEditor.tsx` (~900 lines, six node types in one file). | Low | Medium | Open |
| A7 | Only the Windows `uv.exe` is bundled, so the Python features do not work on macOS/Linux. | Medium | Low | Open (documented in README) |

## Documentation

| ID | Finding | Status |
| --- | --- | --- |
| D1 | No architecture overview, Tauri command reference or format support matrix. | PR (`docs/ARCHITECTURE.md`) |
| D2 | README does not mention tests or the typecheck script. | PR |
| D3 | No contributor guide (branching, PR checklist, GUI screenshot expectations). | Open |

## Found later, while working on the 2D depiction

These came out of the wedge and label work (#14, #15, #16) rather than the
original sweep, and are recorded here so they are not lost.

| ID | Finding | Sev | Risk | Status |
| --- | --- | --- | --- | --- |
| L1 | Nine `react-hooks/exhaustive-deps` warnings. Three are worth looking at properly: `BondsPick2D` (a pointer handler closes over `model`), `FitToContent2D` (fit reads `model.atoms`) and `AromaticCircles2D` (a click handler closes over `hoverCenter` and the toggles) - each can act on a stale value. `CapJoinLine` ignores a changed `color`. The rest (`Bonds2D`, `Atoms2D`, `LabelEditor2D`, `JoinCaps2D`) are stable references or a spare dependency. Adding every dependency blindly changes how often listeners are rebuilt, so each wants its own look. | Medium | Medium | Open |
| L2 | `ExportSvg2D` is not mounted anywhere, so SVG export cannot be reached from the GUI at all. The comment in `StructureCanvas.tsx` says the overlay UI was removed and the functionality "remains available in the component file". Either give it a place (the Fit / Open buttons at the bottom left are the obvious home) or drop it. | Medium | Low | PR #43 |
| L3 | The `greet` Tauri command is registered but never called from the frontend - left over from the Tauri template. | Low | Low | Open |
| L4 | `acsPxOptions` became unused when the editor and the export moved to one options builder (#15). Pixel-unit layouts are still supported by `layout2d`; retiring that mode would remove a whole class of unit confusion - it is what made an SVG of a layout disagree with the canvas. | Low | Medium | Open |
| L5 | Two barrel files nothing imports: `StructureEditor/store/slices/index.ts` and `MoleculeViewer/components/index.ts`. Their modules are imported directly. | Low | Low | Open |
| L6 | Bonds are trimmed at a labelled atom by font size, not by the width of the label, so a bond can run into the hydrogens of a wide label (`NH2`, `OTBS`) in a crowded drawing. | Low | Medium | Open |
| L7 | A label's text metrics differ between the canvas (measured with canvas metrics) and the SVG export (the viewer's font), so hydrogens beside a symbol can sit a fraction differently. The symbol itself is on the atom in both. | Low | Medium | Open |
| L8 | `joinStyle` ("round" / "sharp") has no control in the GUI. | Low | Low | Open |

## Suggested issue order (after this PR and #2 land)

1. **S1 + A5** — Harden sidecar/uv commands: resolve paths in Rust from enums; reject traversal. Add Rust unit tests for path validation.
2. **B7 + S3** — Reap exited sidecars, kill children on app exit, avoid `unwrap()` on the process mutex.
3. **B9 + G1** — User-visible import errors (toast or inline message) and a visible "Open" action in the 2D editor.
4. **G5** — Decide a policy for inactive heavy views (unmount via `keepAlive: false`, or share one renderer).
5. **S2** — Introduce a CSP and verify every view renders.
6. **G2 + G3** — Tab bar polish: close on click, keyboard focus, clearer "New…" menu labels.
7. **A2 → A1** — Type tab data per kind, then converge on one chemistry model.
8. **G4** — Mark the workflow editor as a preview and persist node parameters in tab data.
9. **T4, B8, G6, G7, A6, D3** — Smaller clean-ups.

## Found by driving the built app (see `scripts/gui/`)

| ID | Finding | Sev | Status |
| --- | --- | --- | --- |
| H1 | **Fit to content clips labels at the edge.** `FitToContent2D` takes its bounding box from atom positions alone, so anything a label reaches past its atom falls outside the fit. Opening `scripts/gui/fixtures/depiction-check.mol` and pressing Fit leaves the `H` of `H2N` running off the left edge - column 0 of the window is solid ink. `layout2d`'s own `Layout.bounds` does account for labels; the camera fit does not use it. | Medium | Fixed (PR) |
| H2 | **Some atoms cannot be picked up at all.** `Atoms2D` places its pick circles by rewriting an `InstancedMesh`'s instance matrices and never tells three.js they moved. `InstancedMesh.raycast` first tests a bounding sphere that it computes on the first raycast and then keeps, so an atom outside that sphere is never hit. On a Mac, with `depiction-check.mol` open, the `OH` oxygen of the hashed-wedge centre could be dragged; a chain carbon, a chain end and the wedge stereocentre could not, though each lit its hover ring, which is worked out another way. Recomputing the sphere after the matrices change makes the chain carbon pick up. `BondsPick2D` has the same shape. Drawing ran into the same stale sphere before and was worked around with `frustumCulled={false}`; picking was not. | High | Fixed (#23) |
| H3 | **A wavy bond is notched at every joint.** `Bonds2D` draws a wavy bond as a run of separate straight quads, and nothing fills the wedge-shaped gap between neighbours on the outside of each bend. Anti-aliased, each gap is a pale nick in the line: visible at fit-to-content size, plain once zoomed (`open-and-look`, `03-fitted` and `04-zoomed`). | Medium | Fixed (#31) |
